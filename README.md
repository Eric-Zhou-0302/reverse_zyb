# Reverse Crypto

基于 VWAP 的加密货币均值回归策略回测项目。

## 运行

安装依赖后执行：

```bash
pip install -r requirements.txt
python main.py
```

请将所需市场数据放入 `data_set/`，并按 `base_config.json` 配置回测参数。

## 许可证

本项目采用 [MIT License](LICENSE) 开源。© 2026 Eric Zhou
